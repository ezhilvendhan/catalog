$('#productModal').modal('hide');

$modal = $('#editModal'),
$modalBody = $('#editModal .modal-body'),
$modalHeading = $('#editModal .modal-heading');
$modalHeading.html("<%= @product.name %>");
$modalBody.html("<%= escape_javascript(render 'form') %>");
$modal.modal();
